// # test if database work or not (Delete)

/*
// Import the blueprint we just created
const Test = require('../models/testModel');

// This function finds all documents using the Test model and sends them back
exports.getTestData = async (req, res) => {
  try {
    const data = await Test.find(); // .find() gets everything
    res.json(data);
  } catch (error) {
    res.status(500).json({ message: 'Server error when fetching data' });
  }
};
*/