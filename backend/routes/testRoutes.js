// # test if database work or not (Delete)

/*
const express = require('express');
const router = express.Router();

// Import the controller with our logic
const testController = require('../controllers/testController');

// Create the route: when a GET request is made to the base URL ('/'),
// run the getTestData function from our controller.
router.get('/', testController.getTestData);

module.exports = router;
*/