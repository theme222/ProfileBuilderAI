// # test if database work or not (Delete)

/*
const mongoose = require('mongoose');

const testSchema = new mongoose.Schema({
  // We're just defining that the Member field will hold a String
  Member: String,
});

// Here, we tell Mongoose to use your specific collection name 'testdata'
module.exports = mongoose.model('Test', testSchema, 'testdata');
*/